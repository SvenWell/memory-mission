"""Component 2.3 — CRM Update Agent.

TODO (Step 13, Phase 3): After each interaction, generate structured CRM
entries. Output as .md + JSON (CRM-agnostic). Push to Monday.com/Notion/Salesforce
via Composio.

Principle: CRM data is stored as .md in the firm wiki (knowledge graph is source
of truth). CRM systems are downstream consumers, not sources.
"""
