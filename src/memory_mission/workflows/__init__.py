"""Components 2.1 (Meeting Prep), 2.2 (Email Draft), 2.3 (CRM Update), 2.4 (Real-Time Interaction).

Ships in Phase 2 Steps 8-11.

Customer-facing workflows. What the pilot firm's users actually interact with.
Each reads from employee memory + firm wiki, drafts output, never sends without approval.
"""
